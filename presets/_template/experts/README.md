# Эксперты (эталонный код)

Готовые фрагменты для `expert/save` лежат в пресетах-эталонах:

- [`../github/experts/`](../github/experts/)
- [`../stripe/experts/`](../stripe/experts/)

В шаблоне `_template` код не дублируется — скопируйте из `github` или `stripe` и адаптируйте.
