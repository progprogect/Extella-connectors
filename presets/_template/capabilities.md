# Возможности: `<connector_id>`

Матрица: операция → метод API → требуемые права / scope.

| Операция (продукт) | API (метод / endpoint) | Права / scope | Примечание |
|--------------------|-------------------------|---------------|------------|
| _Пример: список ресурсов_ | `GET /v1/resources` | `read` | Пагинация по курсору |
| | | | |

## Лимиты и квоты

- Rate limits: см. концепт `concepts/api_limits.md`
- Ошибки `429` / `403`: см. концепт troubleshooting

## Вне scope этого пресета

- Перечислите, чего сознательно нет (например admin-only API)
